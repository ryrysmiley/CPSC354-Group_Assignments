## File generated by the BNF Converter (bnfc 2.9.4).

# Makefile for building the parser and test program.

GHC        = ghc
HAPPY      = happy
HAPPY_OPTS = --array --info --ghc --coerce
ALEX       = alex
ALEX_OPTS  = --ghc

# List of goals not corresponding to file names.

.PHONY : all clean distclean

# Default goal.

all : TestNumbers

# Rules for building the parser.

AbsNumbers.hs LexNumbers.x ParNumbers.y PrintNumbers.hs TestNumbers.hs : numbers.cf
	bnfc --haskell numbers.cf

%.hs : %.y
	${HAPPY} ${HAPPY_OPTS} $<

%.hs : %.x
	${ALEX} ${ALEX_OPTS} $<

TestNumbers : AbsNumbers.hs LexNumbers.hs ParNumbers.hs PrintNumbers.hs TestNumbers.hs
	${GHC} ${GHC_OPTS} $@

# Rules for cleaning generated files.

clean :
	-rm -f *.hi *.o *.log *.aux *.dvi

distclean : clean
	-rm -f AbsNumbers.hs AbsNumbers.hs.bak ComposOp.hs ComposOp.hs.bak DocNumbers.txt DocNumbers.txt.bak ErrM.hs ErrM.hs.bak LayoutNumbers.hs LayoutNumbers.hs.bak LexNumbers.x LexNumbers.x.bak ParNumbers.y ParNumbers.y.bak PrintNumbers.hs PrintNumbers.hs.bak SkelNumbers.hs SkelNumbers.hs.bak TestNumbers.hs TestNumbers.hs.bak XMLNumbers.hs XMLNumbers.hs.bak ASTNumbers.agda ASTNumbers.agda.bak ParserNumbers.agda ParserNumbers.agda.bak IOLib.agda IOLib.agda.bak Main.agda Main.agda.bak numbers.dtd numbers.dtd.bak TestNumbers LexNumbers.hs ParNumbers.hs ParNumbers.info ParDataNumbers.hs Makefile


# EOF
